[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[functions."ask"]
  timeout = 26

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
